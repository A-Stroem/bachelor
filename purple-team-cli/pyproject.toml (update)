[tool.poetry]
name = "purple-team-cli"
version = "0.1.0"
description = "A CLI for the purple team"
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
some-dependency = "^1.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"